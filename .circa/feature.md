# Feature Directives

<!--
Human-controlled file. Write directives here to steer the continuous loop.
The loop reads this each cycle and acts on the first unprocessed [ ] item.

Directive statuses (the loop or the human updates these):
  [ ] — pending, will be processed on the next cycle
  [>] — queued as a task in queue.md (agent added this automatically)
  [?] — needs clarification (see flags.md for the agent's question)
  [x] — completed

Examples:
  - [ ] Add input validation to all public API endpoints
  - [ ] Improve test coverage for the auth module to > 80%
  - [ ] Research best practices for rate limiting in Python
  - [ ] Refactor db/session.py to use async/await
-->

## Active Directives
<!-- Add your directives below. The loop processes them top-to-bottom, one per cycle. -->
